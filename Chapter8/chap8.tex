% Kapitel 8 - Diskussion
\section{Functional Requirements}
{\large 
	Requirement engineering is essential at the beginning of every project. Formulating and documenting precisely defined requirements is a cornerstone in the course of realization. In this way, mundane mistakes can be avoided and the vision of the final product is clear. Thus this chapter will explain all functional requirements of the finshed product.
	
	


	
	
}
\subsection{ECG-Data}
\begin{center}
	\includegraphics[scale = 1.0]{/Users/eric/Downloads/Patient2Phone.png}
	\label{fig:Patient2Phone}
	\captionof{figure}{Informationtransfer}
\end{center}
{\large
The application receives integer values from the microcontroller, which represent the heartbeat of the person connected to the hardware. These values must be mapped onto the screen to provide a clear and accurate visualization of the heart’s activity.
A key challenge in this process is ensuring that the visualization adapts to different screen sizes. Since the application may run on devices with varying display dimensions and resolutions, a static mapping approach is insufficient. Instead, a dynamic and scalable algorithm is required to transform the received data into a graphical representation that maintains consistent proportions and readability across all screen sizes.
To achieve this, the application must implement a flexible coordinate transformation. The incoming data points need to be scaled appropriately based on the available screen width and height while maintaining the correct aspect ratio of the graph. Additionally, the algorithm should support real-time updates to provide an accurate and responsive user experience. The transformation of raw data points must be performed quickly, otherwise, there is a risk of data loss.
Therefore many parameters are need to be considerd when transforming the data. \mbox{}\\
\underline{\textbf{Parameters}}\mbox{}\\
\begin{itemize}
	\item Screen dimensions (height, width)
	\item Current min and max value in buffer
	\item Buffer capacity
	\item  $\Delta v \quad ,where \; \Delta v =  \max - \min$ (range of values in current Buffer)
	\\
	\item coefficient = $\dfrac{\text{height} - 2 \times \text{shiftH} }{\Delta v}$ (maps raw values to screen height, with margin of shiftH pixels at the top and bottom)
	\\
	\item step = $\frac{width}{bufferCap.}$(Determines horizontal space for each data point)
\end{itemize}

\mbox{}\\
all data in the buffer will be updated as shown in the following code snippet:\mbox{}\\
\\
 \begin{lstlisting}[label={fig:simpleSourceGen.cs}, caption={Einfaches Beispiel eines Sourcecode-Generators, abgeleitet von \cite{microsoft_roslyndocsfeaturesincremental-generatorsmd_2024, carter_introducing_2020} und eigenen Ergänzungen}]
 	for (i in dataTemp.indices) {
 		data[i] = (maxV - dataTemp[i]) * coeff + shiftH
 	}
 \end{lstlisting}
 The calculation process can be examined in greater detail in the following example, which demonstrates how each data point is transformed and mapped onto the screen.
\\
To keep the example simple and concise, we will deal with an array of size 5 and a Coordinate system with a range of (6,6): \mbox{}\\ 

\begin{center}
\newcommand{\sep}{\hspace*{.5em}}
\noindent
$\fbox{951} \sep \fbox{960} \sep \fbox{980} \sep \fbox{1050}\sep \fbox{930}$
\mbox{}\\
\begin{itemize}
	\item Screen dimensions (6,6)
	\item minV = 930, maxV = 1050
	\item BufferCapacity = 5 
	\item  $\Delta v = 1050-930$
	\\
	\item coefficient = $\dfrac{6 - 2 \times 1 }{120}$ (shiftH = 1 )
	\\
	\item step = \large{$\frac{6}{5}$}
\end{itemize}
\mbox{}\\

\end{center}
\begin{center}
The newly evaluated Array contains the following data:

\newcommand{\sep}{\hspace*{.5em}}
\noindent

$\fbox{4.4} \sep \fbox{4.0} \sep \fbox{3.3} \sep \fbox{1.0}\sep \fbox{5.0}$\mbox{}\\
\mbox{}\\
Resulting in the follwing drawing:
\end{center}

\begin{tikzpicture}
	% Draw axes
	\draw[thick,->] (0,0) -- (6,0) node[anchor=north] {Width (6,0)};
	
	\draw[thick,->] (0,0) -- (0,-6) node[anchor=east] {Height (0,6)};
	
	% Labels
	\node[anchor=north east] at (0,0) {Origin (0,0)};
	
	% Example data points
	\fill (1.2, -4.3) circle (2pt) node[anchor=north] {P1};
	\fill (2.4,-4.0) circle (2pt) node[anchor=north] {P2};
	\fill (3.6,-3.3) circle (2pt) node[anchor=north] {P3};
	\fill (4.8,-5) circle (2pt) node[anchor=north] {P4};
	\fill (6.0,-1) circle (2pt) node[anchor=north] {P5};
	% Connect points (example ECG line)
	\draw[thick] (1.2,-4.3) -- (2.4,-4.0) -- (3.6,-3.3) -- (4.8,-5) -- (6.0, -1);
\end{tikzpicture}


\mbox{}\\

\mbox{}\\
Resulting in the follwing Complexity Analysis: 
\begin{itemize}
	\item Data Conversion: O(n) (for n data points)
	\item Scaling \& Mapping: O(n) for n data points)
	\item Overall Complexity: O(n) linear (for n data points)
\end{itemize}
The resulting linear Complexity of O(n) makes the algorithm efficient for real-time processing. 


}
\subsection{Drawing ECG-Data in realtime}
{\large
	 Processing real-time electrocardiographic (ECG) data presents multiple challenges that must be addressed to ensure accurate and efficient visualization. It is helpful to split this rather complex functionality into multiple concerns:
	\begin{itemize}
		\item The sample rate of the microcontroller (1)
		\item Consume and process incoming data simultaneously (2)
		\item Datahandling and Drawing in parallel (3)
	\end{itemize}
	These issues must be viewed and solved isolated, and subsequently combine each solved problem with the other. It is important to mention that some issues may create restrictions for other issues. Therefore, it is essential to implement one task at a time and connect them all afterward.
	
	
	
	{\textbf{\underline{(1) Handle The Sample rate of the microcontroller}}
	
	The microcontroller sends 10 samples every 8 milliseconds (1000ms = 1s). \\
	\\
	$\dfrac{1000}{8}$ = $\dfrac{125}{1}$   $\widehat{=}$   $125 Hz$ \\
	\\
	Hence we have  to process a sample rate of: $f(t)= 125 \times t $, t in seconds. 
	If handling this datastream is not possible, data will be lost.
	
	
	
	
	\textbf{\underline{(2)Consume and process incoming data at the same time}}\\
	\\
	To consume and process data at the same time,  spliting up processing and consuming into two indepent tasks by assigning each task to its own thread, is necessary. Making it possible to operate in paralell.
	
	Since both threads  have to access the same ressource at the same time, we have to make use of mutual exclusion. 
	
	
	
	\hfil \break
	\hfil \break
 	\textbf{\underline{(3)Datahandling and drawing in parallel }}\\
 	\\
 	Thus the drawing entity has to countiously read data, and the datahandler entity receives a continous data stream.
 	The suitable data structure must be able to constantly record new data and retain a certain amount of old data. So that a screen-width drawing can be displayed continuously. In addition, it is important that the data structure is not to large, which would lead to visible latencies. 
 	
 	This led to the Ringbuffer beeing the datastructure of choice.
 	Ringbuffers are often used when having to process datastreams. The ringbuffer has a fixed size, thus old data gets overwritten. Therefore, there is no need to worry about the data structure consuming too much memory.
 	
	
	
	
}




\subsection{24-Hour recording}

The functional requirements of the 24-Hour recording are shown in the list below. All of the listed requirements will be explainend in detail in chapter \ref{sec:realization}.
	\begin{itemize}
		\item Recording mode
		\item Filechooser conzept, to choose multiple files
		\item Start a recording, and assigning a name
		\item Abort a recording 
		\item Display 24-Hour long recorded ECG-Data
		\item Show the current time intervalls
		\item Zoom into the displayed data, zooming out of the displayed data
		\item Plot BPM of the current displayed time intervall
	\end{itemize}

\subsection{Connection via Bluetooth Low Energy }
{\large
	The application is required to establish and maintain a reliable Bluetooth Low Energy (BLE) connection with the hardware. A stable connection is essential for continous datatransfer aswell as for the 24-Hour recording Mode.  
	The BLE entity must be capable of the following items:
		\begin{itemize}
		\item Continous datastream
		\item Communicate with the application
		\item Share current the current mode (realtime, or recording)
		\item Scan for near devices
		\item split \& reassamble to large datasets 
	\end{itemize}
	
	
}
